name: gps_tracker
version: 1.0.0+1
description: Cross-platform GPS tracker app for Android and iOS.
publish_to: 'none'

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^12.0.0
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  intl: ^0.19.0

flutter:
  uses-material-design: true
